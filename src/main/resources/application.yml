server:
  port: ${PORT_NUM}
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  profiles:
    include:
      - oauth
      - db
      - mail
      - aws

jwt:
  secret: ${JWT_SECRET}
